# Shared CTGTRec model configuration.
# Dataset-specific final values are loaded from configs/final/ctgtrec/.

embedding_size: 64
feat_embed_dim: 64

n_mm_layers: 1
n_ui_layers: 1
knn_k: 10
visual_fusion_weight: 0.1

aux_loss_weight: 0.0
ui_edge_dropout: 0.0

temporal_graph_dir: continuous_time_adj
ct_raw_graph_file: ct_raw_adj_user_tau0p3.npz
ct_normalized_graph_file: ct_adj_user_tau0p3.npz

trend_weight: 1.10
trend_recent_ratio: 0.25
trend_epsilon: 1.0e-12
trend_clip: 3.0

hyper_parameters: []
