# CTGTRec final configuration: Baby
# Fixed after validation-set model selection; not a search space.

learning_rate: 5.0e-4
n_ui_layers: 5

ct_raw_graph_file: ct_raw_adj_user_tau0p3.npz
ct_normalized_graph_file: ct_adj_user_tau0p3.npz

trend_weight: 1.10
trend_recent_ratio: 0.45

aux_loss_weight: 1.0e-3
ui_edge_dropout: 0.0

hyper_parameters: []
